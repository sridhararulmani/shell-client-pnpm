export class AppMenuData {
  constructor(id, title, startIcon, endIcon, url) {
    this.id = id;
    this.title = title;
    this.startIcon = startIcon;
    this.endIcon = endIcon;
    this.url = url;
  }
}
